--- 
build: 
  script: 
    - rm -rf output
    - pelican content
  stage: build
  tags: 
    - ci
    - shell
    - ssh

deploy:
  script:
    - cd output;nohup python -m pelican.server &
    - cd pelican content -o output -s pelicanconf.py
  stage: deploy
  tags:
    - ci
    - shell
    - ssh
  when: manual

stages: 
  - build
  - deploy
